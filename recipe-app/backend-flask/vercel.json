{
    "version": 2,
    "env": {
        "FLASK_ENV": "production",
	    "ENV": "prod"
    },
    "devCommand": "gunicorn wsgi:application --bind 0.0.0.0:5000",
    "buildCommand": "gunicorn wsgi:application"
}
